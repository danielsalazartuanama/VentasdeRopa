@model VASTQuickShoping.Models.Domain.Category
@{
    ViewBag.Title = "Formulario";
}

@using (Html.BeginForm((string)ViewBag.op, "Category", FormMethod.Post))
{
    @Html.AntiForgeryToken()

    if (Model !=null)
    {
        @Html.HiddenFor(x => x.CategoryID)
    }
}

